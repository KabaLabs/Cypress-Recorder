{
  "name": "cypress-recorder",
  "version": "0.0.1",
  "description": "A chrome developer tool that records user interaction with a web application and generates cypress code to allow the developer to replicate that particular session.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^2.3.3",
    "@typescript-eslint/parser": "^2.3.3",
    "eslint": "^6.5.1",
    "typescript": "^3.6.4"
  }
}
