import * as React from 'react';
import { shallow } from 'enzyme';
import App from '../popup/components/App';

import '../setupTests';

describe('App', () => {
  let wrapper;
  it('.', () => {
    // wrapper = shallow(<App />);
  });
});
