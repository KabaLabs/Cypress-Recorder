// unit tests for e recorder
const recorder = require("../event-recording/EventRecorder.ts");

const sum = require("../event-recording/EventRecorder.ts")

test("adds that shit up", () => {
    expect(sum(1,2)).toBe(3);
});




// require in file to be tested
// const htmlTest = require('../popup/index.html');

// // test that is irrelevent
// describe('title', () => {
//     it('has an h1 tag with the text cypress recorder', () => {
//         const location = htmlTest.document.getElementById("title");
//         expect(location.tagName).toBe("H1");
//         expect(location.innerHTML).toBe("Cyress Recorder");
//     })
// })