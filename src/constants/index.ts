export enum EventType {
  CLICK = 'click',
  CHANGE = 'change',
  DBLCLICK = 'dblclick',
  KEYDOWN = 'keydown',
  SUBMIT = 'submit',
  RCLICK = 'contextmenu',
}

export enum ControlAction {
  START,
  STOP,
  RESET,
  DELETE,
  MOVE,
  PUSH,
}

export enum RecState {
  ON,
  OFF,
  PAUSED,
}
